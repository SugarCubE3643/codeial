<li>
    <p>
        <% if(locals.user && (locals.user.id == comment.user.id)) { %>
             <!--|| locals.user.id == post.user.id (authorization part) -->
            <small>
                <a href="/comments/destroy/<%= comment._id %> ">delete</a>
            </small>
        <% } %> 
        <%= comment.content %>
        <br>
        <small>
            <%= comment.user.name %> 
        </small> 
    </p>
</li> 